import React, { Component } from 'react';
class reset extends Component {
    render() {
        return (
            <button type="button" className="btn btn-primary warning">Reset</button>
        );
    }
}

export default reset;
